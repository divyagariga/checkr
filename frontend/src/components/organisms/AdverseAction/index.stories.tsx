import type { Meta, StoryObj } from '@storybook/react';
import AdverseAction from '.';

const meta: Meta<typeof AdverseAction> = {
  title: 'Organisms/AdverseAction',
  component: AdverseAction,
};

export default meta;
type Story = StoryObj<typeof AdverseAction>;

export const AdverseActionComponent: Story = {};
